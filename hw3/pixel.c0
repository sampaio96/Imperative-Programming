#use <util>
#use <conio>

typedef int pixel;

void printhex(int x) {
  println(string_join("0x",int2hex(x)));
}
/*

// 5x5 image
A[b]
i = b/5;
j = b%5;

b=i*5+j

// width * height
A[b]
i = b/width
j = b%width

These functions should create a new pixel array with different contents, and not make any changes to the original array.

*/